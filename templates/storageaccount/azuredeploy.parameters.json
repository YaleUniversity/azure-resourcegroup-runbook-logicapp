{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {

    "SkuName" : { "value": "SkuName" },
    "AccountKind" : { "value": "{{ AccessTierDefault }}" },
    "AccessTierDefault": {"value": "{{ AccessTierDefault }}"},

    "CustomDomain": { "value": "{{ CustomDomain }}" },

    "ResourceLocation": {"value": "{{ ResourceLocation }}"},
    "OwnerNetId": {"value": "{{ OwnerNetId }}"},
    "OwnerDepartment": { "value": "{{ OwnerDepartment }}" },
    "OwnerDepartmentContact": { "value": "{{ OwnerDepartmentContact }}" },
    "ChargingAccount": { "value": "{{ ChargingAccount }}" },
    "DataSensitivity": { "value": "{{ DataSensitivity }}"},
    "Environment": { "value": "{{ Environment }}" },
    "Application": { "value": "{{ Application }}" }

    }
}
