{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {

    "SkuName" : { "value": "{{ SkuName }}" },
    "AccountKind" : { "value": "{{ AccountKind }}" },
    "AccessTierDefault": {"value": "{{ AccessTierDefault }}"},
    "CustomDomain": { "value": "{{ CustomDomain }}" },

    "ResourceLocation": {"value": "{{ ResourceLocation}}"},
    "ApplicationName": { "value": "{{ ApplicationName}}" },
    "ApplicationBusinessUnit": {"value": "{{ ApplicationBusinessUnit }}"},
    "Environment": { "value": "{{ Environment }}" }

  }
}